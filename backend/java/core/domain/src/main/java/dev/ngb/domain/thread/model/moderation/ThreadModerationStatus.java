package dev.ngb.domain.thread.model.moderation;

/**
 * Lifecycle states for a thread moderation report.
 */
public enum ThreadModerationStatus {
    PENDING,
    REVIEWING,
    RESOLVED,
    DISMISSED
}
